name: Nico OS inbox guard

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  maintenance-hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nico OS inbox guard temporarily paused for queue cleanup"
